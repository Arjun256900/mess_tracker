<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Account Create</title>
    <link rel="shorcut icon" href="/assets/favicon.ico" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap"
      rel="stylesheet"
    />

    <link rel="stylesheet" href="/styles.css" />
    <body>
      <div class="container">
        <form
          class="form"
          id="createAccount"
          action="/register/submit"
          method="POST"
        >
          <h1 class="form_title">Create Account</h1>
          <div class="form_message form_message--error"></div>
          <div class="form_input-group">
            <input
              type="text"
              id="signupUsername"
              class="form_input"
              autofocus
              name="username"
              placeholder="Username"
            />
            <% if(locals.errors && locals.errors.username) {%>
            <p class="error"><%= errors.username.msg %></p>
            <% }%>
          </div>
          <div class="form_input-group">
            <input
              type="text"
              class="form_input"
              id="signupEmail"
              autofocus
              placeholder="Email Address"
              name="email"
            />
            <% if(locals.errors && locals.errors.email) {%>
            <p class="error"><%= errors.email.msg %></p>
            <% }%>
          </div>
          <div class="form_input-group">
            <input
              type="password"
              class="form_input"
              autofocus
              placeholder="Password"
              id="signupPassword"
              name="password"
            />
            <% if(locals.errors && locals.errors.password) {%>
            <p class="error"><%=errors.password.msg %></p>
            <% }%>
          </div>
          <div class="form_input-group">
            <input
              type="password"
              class="form_input"
              autofocus
              id="signupConfirmPassword"
              placeholder="Confirm password"
              name="confirm"
            />
            <% if(locals.errors && locals.errors.confirm){%>
            <p class="error"><%=errors.confirm.msg %></p>
            <% }%>
          </div>
          <button class="form_button" type="submit">Continue</button>
          <p class="form_text">
            <a class="form_link" href="/login" id="linkLogin"
              >Already have an account? Sign in</a
            >
          </p>
        </form>
      </div>
    </body>
  </head>
</html>
